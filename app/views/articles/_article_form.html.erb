<%= form_for @article do |f| %>

<%= f.label :title %>
<%= f.text_field :title %>
<%= f.label :content %>
<%= f.text_area :content %>

<%= f.select :category_id, Category.all.map {|c| [c.category, c.id]} %>

<%= f.submit %>

<% end %>